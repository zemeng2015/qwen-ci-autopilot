diff --git a/example/target-file b/example/target-file
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/example/target-file
@@ -0,0 +1,5 @@
+Patch intent is generated by the autopilot after risk review.
+High-risk runs keep this artifact blocked until a human approves the plan.
+The demo app does not write into a customer repository by default.
+This file exists so judges can inspect the expected artifact shape.
+Live integrations can replace it with a real repo diff.
